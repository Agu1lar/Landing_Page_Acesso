# Hooks leves para desenvolvimento solo. Pule com: git commit --no-verify

commit-msg:
  jobs:
    - name: commitlint
      run: npx --no -- commitlint --edit {1}

pre-commit:
  jobs:
    - name: lint
      run: npm run lint
      glob:
        - '**/*.{js,jsx,ts,tsx}'
